package com.example.data_android.local

//class LocalDataSourceImpl @Inject constructor(private val dao: CharacterDao) {
//    suspend fun getAllCharacters(): List<MyResponseDTO> {
//        return dao.getAllCharacters().map { it.toMyResponseDTO() }
//
//    }
//
//    suspend fun insertAllCharacters(characterList: List<MyResponseDTO>) {
//        dao.insertAllCharacters(characterList.map { it.toMyResponseEntity() })
//    }
//}